class Product {
  constructor(id, title, imageUrl, description, rating, price) {
    this.id = id;
    this.title = title;
    this.imageUrl = imageUrl;
    this.description = description;
    this.rating = rating;
    this.price = price;
  }
}

export default Product